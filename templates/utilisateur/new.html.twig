{% extends 'admin/base.html.twig' %}
{% block title %}Nouvel Utilisateur{% endblock %}

{% block body %}
<div class="page-heading">
    <div class="page-title-headings">
        <h3>Ajouter un Utilisateur</h3>
        <a href="{{ path('app_utilisateur_index') }}" class="btn btn-outline-secondary btn-sm">
            <i class="bi bi-arrow-left me-1"></i> Retour à la liste
        </a>
    </div>
</div>

<section class="section">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title"><i class="bi bi-person-plus text-primary me-2"></i>Nouvel Utilisateur</h4>
        </div>
        <div class="card-body">
            {{ include('utilisateur/_form.html.twig') }}
        </div>
    </div>
</section>
{% endblock %}
